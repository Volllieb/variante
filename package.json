{
  "name": "variante",
  "private": true,
  "scripts": {
    "dev:tool":     "cd ab-tool && npm run dev",
    "build:tool":   "cd ab-tool && npm run build",
    "build:plugin": "cd figma-plugin && npm run build",
    "build:all":    "npm run build:tool && npm run build:plugin"
  }
}
